# 火影战力计算器

一个纯前端、非官方的同人工具，包含“战力计算器”“升级时间计算”“抗魔计算器”和“消耗材料查询”四个板块，均可从侧边栏进入。战力板块支持等级、忍魂、天赋、装备、勾玉、饰品与符文、神器、通灵、忍具、秘卷、装扮、称号和头像框等 16 个战力分项；升级板块可推演 140–175 级的升级天数、完成日期与经验来源；抗魔与材料板块用于规划饰品强化和掉落门槛。

在线使用：[GitHub Pages](https://liang-jiahe.github.io/powerscaling-tools/)

## 功能

- 网站根地址为独立浅色工具首页，仅展示“工具箱（4 个工具）”与四张紧凑卡片，不显示侧边栏、顶部工具栏或手机导航
- 进入工具后侧边栏只显示当前工具；抗魔计算器与消耗材料查询合并显示为两个入口
- 工具与分项支持可收藏的地址、刷新恢复和浏览器前进/后退，旧锚点链接继续可用
- 所有计算均在浏览器本机完成，不上传数据
- 统一的 TypeScript 纯函数计算引擎
- 20–170 级等级收益表、18 种通灵兽数据和 11 档符文共鸣
- 全局收集加成实时联动
- 总战力、可归属面板属性和未归属直接战力分别展示
- 140–170 级升级经验、丰饶、活跃、精英与修罗参考数据表，以及基于其增量规则推算的 171–175 级数据
- V10 以下、V10–V13、V14–V15 三档丰饶收益
- 支持 VIP、超影、每日购体和每周体力礼包的逐日升级推演
- 计算六个饰品部位的强化后抗魔，展示 10 个品级的掉落门槛与目标差距
- 支持饰品名称记录、更换标记、目标选择和独立重置
- 查询破晓、晨曦、旭日、苍穹、云迹五套材料表，汇总绿水、紫星与保护符
- 按“单次消耗 / 此前累计消耗”分组展示材料，支持 +0 到最高 +35 的区间查询
- 战力数据在本机自动保存；升级、抗魔与材料配置只在当前会话中保留，切换板块不丢失输入
- 战力导航默认折叠；顶部下载、导入、公式、PDF 与清空工具栏仅在战力板块显示
- 可直接下载带中文文本和完整录入明细的 PDF 战力报告
- 桌面、平板和手机响应式布局

## 页面地址

GitHub Pages 项目根路径 `/powerscaling-tools/` 显示首页；每个工具都有独立的 HTML 文件地址，刷新不会产生子路径 404：

- 首页：`/powerscaling-tools/`
- 战力计算器：`/powerscaling-tools/power.html`
- 战力分项（示例）：`/powerscaling-tools/power.html#tools`
- 升级时间计算：`/powerscaling-tools/upgrade.html`
- 抗魔计算器：`/powerscaling-tools/shipin/km_cal.html`
- 消耗材料查询：`/powerscaling-tools/shipin/materials.html`

旧的 `#/power`、`#/upgrade`、`#/magic`、`#/materials` 哈希链接仍可打开对应工具。首页卡片可进入对应工具，战力分项会自动展开并高亮侧边栏条目。工具页保留侧边栏与手机导航，可通过“首页”或手机底部首页按钮返回根地址，输入数据在切换时保留。首页卡片在桌面（≥1024px）为三列、平板（640–1023px）为两列、手机（<640px）为一列。刷新会恢复当前页面和分项位置；升级、抗魔与材料的输入仍遵循本次会话规则，刷新后恢复默认，不会新增持久化。

## 本地开发

```bash
npm install
npm run dev
```

验证：

```bash
npm test
npm run build
```

## 核心公式

```text
生命 × 1
攻击 × 3.5
防御 × 11.6
暴击 / 元素攻击 × 6
抗暴 / 元素防御 × 10

收集倍率 = 1 + 全收集加成 × (1 + 对应属性百分比加成)

单件总抗魔 = 向上取整(基础抗魔 × (1 + 5% × 强化等级))
区间强化材料 = 当前等级 + 1 至目标等级（含）的逐级消耗之和
```

具体模块是否吃收集加成，以页面导出的《公式说明》及 `src/domain/engine.ts` 为准。

## 数据说明

战力等级、通灵、符文共鸣表及升级经验数据依据参考工具中公开呈现的事实性数值整理。升级算法与数据来自本地参考项目 `E:\hyrz`，以独立的 React + TypeScript 组件重新实现；应用代码、状态结构和猫咪主题视觉均独立编写。

抗魔算法、饰品门槛与五套强化材料数据整合自 [meow-magic-calculator](https://github.com/liang-jiahe/meow-magic-calculator)，对应提交 [`d6766f7`](https://github.com/liang-jiahe/meow-magic-calculator/tree/d6766f7aa5295ae367529c3de877e07506ca3651)。原始 Excel 数据表可在该提交的 [`data/source`](https://github.com/liang-jiahe/meow-magic-calculator/tree/d6766f7aa5295ae367529c3de877e07506ca3651/data/source) 追溯。新板块使用原生 React + TypeScript 组件，与原项目样式和状态隔离，不依赖外部页面或运行时请求；运行时材料数据位于 `src/magic/materials-data.ts`。

本项目不隶属于、也不代表任何游戏发行商或版权方；数据与算法仅供玩家交流和估算，不构成官方数值承诺。

PDF 报告使用 Google Fonts 提供的 Noto Sans SC 字体，依据 SIL Open Font License 1.1 使用，许可文本位于 `public/fonts/OFL-NotoSansSC.txt`。

## 许可证

原创程序代码以 [MIT License](LICENSE) 发布。游戏名称、设定及相关商标归其各自权利人所有。
